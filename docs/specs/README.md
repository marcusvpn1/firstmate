# Specs index

Map of topic areas to their spec files.
When authoring a spec, scan this index first to decide whether to append to an existing topic or create a new file (see `.agents/skills/spec-scaffold/SKILL.md` for the granularity guideline and organization convention).

Each spec entry is a frozen point-in-time record, not living documentation.

<!-- No specs yet — add entries below as specs are committed. -->
